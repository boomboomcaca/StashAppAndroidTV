query OllamaStatus {
  ollamaStatus {
    available
    config {
      baseUrl
      model
      timeout
      enabled
      fallbackToTraditionalDict
      promptTemplate
    }
    models
    version
  }
}

